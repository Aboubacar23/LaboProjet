{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ statut.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('statut_index') }}">Retour</a>

    <a href="{{ path('statut_edit', {'id': statut.id}) }}">Modifier</a>

    {{ include('statut/_delete_form.html.twig') }}
{% endblock %}
