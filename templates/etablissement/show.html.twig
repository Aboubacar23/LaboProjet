{% extends 'base.html.twig' %}

{% block title %}Etablissement{% endblock %}

{% block body %}
    <h1>Etablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etablissement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etablissement.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etablissement_index') }}">Retour</a>

    <a href="{{ path('etablissement_edit', {'id': etablissement.id}) }}">Modifier</a>

    {{ include('etablissement/_delete_form.html.twig') }}
{% endblock %}
